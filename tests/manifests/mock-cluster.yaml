# Mock cluster state for e2e tests
# - Creates a namespace labeled env=prod so the operator's namespace-selector can match it.
# - Includes a minimal Service + Deployment + Ingress that ingress2gateway can convert.
---
apiVersion: v1
kind: Namespace
metadata:
  name: ingress-migration-mock
  labels:
    env: prod
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: demo-app
  namespace: ingress-migration-mock
  labels:
    app: demo-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: demo-app
  template:
    metadata:
      labels:
        app: demo-app
    spec:
      containers:
        - name: echoserver
          image: gcr.io/google-containers/echoserver:1.10
          ports:
            - containerPort: 8080
---
apiVersion: v1
kind: Service
metadata:
  name: demo-app
  namespace: ingress-migration-mock
spec:
  selector:
    app: demo-app
  ports:
    - port: 80
      targetPort: 8080
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: demo-ingress
  namespace: ingress-migration-mock
  annotations:
    kubernetes.io/ingress.class: "nginx"
spec:
  rules:
    - host: demo.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: demo-app
                port:
                  number: 80
---
# External database — no spec.selector, so its Endpoints are manually managed.
# This is the kind of resource that must be migrated to EndpointSlice.
apiVersion: v1
kind: Service
metadata:
  name: external-db
  namespace: ingress-migration-mock
spec:
  ports:
    - name: postgres
      port: 5432
      protocol: TCP
---
apiVersion: v1
kind: Endpoints
metadata:
  name: external-db
  namespace: ingress-migration-mock
subsets:
  - addresses:
      - ip: 10.0.0.100
      - ip: 10.0.0.101
    ports:
      - name: postgres
        port: 5432
        protocol: TCP
